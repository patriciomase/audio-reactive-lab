# Domain docs

This repository uses a single-context layout.

Before engineering or architecture work, read:

- `CONTEXT.md` at the repository root, when present.
- Relevant records under `docs/adr/`, when present.

Use the glossary's vocabulary in proposals, issues, tests, and implementation. Surface conflicts with an existing ADR explicitly.
